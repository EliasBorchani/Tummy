package com.tummy.tokens

/**
 * Opacity tokens for state modulation.
 */
object AppOpacity {
    // Standard disabled fade — inputs, surfaces, secondary chrome.
    val Disabled: Float = 0.5f

    // Stronger disabled fade — solid buttons need to read clearly inactive.
    val DisabledStrong: Float = 0.4f
}
